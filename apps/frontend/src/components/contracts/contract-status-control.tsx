"use client";

/**
 * @deprecated A alteração de status foi unificada no formulário de edição do contrato
 * (`ContractForm` / `ContractFormModal`). Mantido apenas para evitar imports quebrados.
 */
export { CONTRACT_STATUS_OPTIONS, contractStatusConfirmationCopy, contractStatusLabel } from "@/modules/contracts/contract-status";
