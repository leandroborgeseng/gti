import type { Prisma } from "@prisma/client";

/**
 * Cláusula `where` do modelo `Ticket` (namespace completo do cliente gerado na raiz do monorepo).
 */
export type TicketWhereInput = Prisma.TicketWhereInput;
