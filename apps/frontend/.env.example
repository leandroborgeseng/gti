# =============================================================================
# Frontend Next (apps/frontend) — copie para .env.local
# =============================================================================

# URL pública desta app (metadata, PWA, links absolutos). Em produção: https://seu-dominio
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# API Nest (contratos, medições, dashboard, etc.)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000/api

# Servidor Node GLPI (Kanban + /api/kanban, etc.). Obrigatório para proxy e rewrites.
# Em desenvolvimento: porta onde corre `npm start` na raiz do monorepo (ex.: 3000).
GLPI_SYNC_ORIGIN=http://localhost:3000
